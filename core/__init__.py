"""
core — shared infrastructure for the recon framework.

Modules:
    exceptions      Structured exception hierarchy (implemented).
    config_manager  Load/validate/write config.json (planned).
    logger          JSON rotating log handler setup (planned).
    database        SQLite schema and query layer (planned).
    checkpoint      Per-stage resume state (planned).
    tool_manager    Tool detection and auto-install (planned).
"""
